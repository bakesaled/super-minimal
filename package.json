{
  "name": "super-minimal",
  "version": "1.0.0",
  "description": "A light-weight, simple, minimal blog template",
  "scripts": {
    "del-dist": "del-cli dist",
    "copy": "npm run del-dist && copyfiles -e \"*.zip\" -e \"node_modules/**/*\" \"**/*\" dist"
  },
  "devDependencies": {
    "copyfiles": "^2.1.1",
    "del-cli": "^2.0.0"
  },
  "dependencies": {}
}